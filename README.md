# Graph-App
Use boost library and Dijkstra's method for finding quickest routes between cities. Use a vector of 20 cities, a BFS function to find routes, and option to run Connected Components algorithm.
